# Notebooks

| Notebook | Status |
|----------|--------|
| `demo.ipynb` | Planned v0.2 — thin wrapper around `arthjax` package |
| `kaggle.ipynb` | Planned v0.5 — GPU kernel for Kaggle |

The full prototype notebook remains local until simulation logic is extracted into `arthjax/`.
